# Complete supported ZoidbergChain node installation.
# The current FastAPI application imports originality/OCR code at startup, so both
# runtime groups are required for a runnable node.
-r requirements-core.txt
-r requirements-originality.txt
